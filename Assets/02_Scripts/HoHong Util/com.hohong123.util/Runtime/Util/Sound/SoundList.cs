namespace Util.Sound {
    // Use real file name.
    public enum SFXList {
        #region ----- Common -----
        Click = 100,
        Notification,
        CashRegister,
        #endregion
    }

    public enum BGMList {
        Stage1 = 11000100,
    }

    public enum SoundType {
        SFX,
        BGM,
    }
}